var playerGuess = document.getElementById("player-guess"),
    guessNumber=document.getElementById("get-number"),
    displayArea= document.getElementById("display-area"),
    scoreKeeper=document.getElementById("score-keeper");

    var compNumber = function compNumber({
         return: Math,floor=(Math.random() * 100) + 1,
    }

    guessNumber.addEventListener = ("click", function(){
        var match= function match ({
             playerGuess.value = compNumber;
         
            
            if(match = true){
                scoreKeeper.innerHTML+= ++1 + "You are Correct!";
                displayArea.innerHTML+= compNumber + ++1;
            }
            else(match = false);{
                scoreKeeper.innerHTML += "" +"Wrong Answer!";
                displayArea.innerHTML+= compNumber + ++1;
            }
        
        }

    });